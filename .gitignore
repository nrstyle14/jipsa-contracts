cache/
out/
broadcast/
.env
.env.*
!.env.example
.DS_Store

# 작업 지시서·원페이저 등 대외 비공개 문서 (공개 원격이라 커밋하지 않는다)
dev_doc/

# Node / pnpm
node_modules/
dist/
*.tsbuildinfo

# 서명된 위임 산출물 (에이전트에 전달하는 것이며 커밋 대상 아님)
delegation.json
delegation.json.*
e2e-delegation.json
